Функция ПолучитьЗадачи(пПроект, пКодМантиса, пДатаПоследнегоОбновления, соединение = Неопределено) Экспорт 
	
	//Если соединение = Неопределено Тогда
	//	Возврат Ложь;
	//КонецЕсли;
	//
	////лСтруктура = MySQLСервер.ПолучитьТаблицуЗадачМантиса(соединение, пКодМантиса, пДатаПоследнегоОбновления);
	////то ли из-за рассинхронизации времени, то ли еще из-за чего не всегда получает задачи
	//лСтруктура = MySQLСервер.ПолучитьТаблицуЗадачМантиса(соединение, пКодМантиса, НачалоДня(пДатаПоследнегоОбновления));
	//
	//Мантис.ЗаписатьЗадачи(пПроект, пКодМантиса, пДатаПоследнегоОбновления, лСтруктура);
	//
	//Возврат лСтруктура.списокЗадач.Количество();
КонецФункции // ПолучитьЗадачи()

Функция ОбновитьВсеПроектыИзМантиса(соединение = Неопределено) Экспорт 
	
КонецФункции // ОбновитьВсеПроектыИзМантиса()


