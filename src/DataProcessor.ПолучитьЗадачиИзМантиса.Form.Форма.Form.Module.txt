
&НаСервере
Функция ПолучитьИзМантиса(пUid)
	//стрОшибки = "";
	//соединение = КэшируемыеФункции.ИницилизироватьADO(, стрОшибки);
	//
	//Если соединение = Неопределено Тогда
	//	Сообщить(стрОшибки);
	//	Возврат Ложь;
	//Иначе
	//	лТаблицаРезультата = MySQLСервер.ПолучитьТаблицуЗадачМантиса(соединение, пUid);
	//КонецЕсли;
	//
	//ЗначениеВРеквизитФормы(лТаблицаРезультата, "ТаблицаРезультата");
КонецФункции // ПолучитьИзМантиса()

&НаКлиенте
Процедура ВыполнитьКоманду(Команда)
	ПолучитьИзМантиса(Объект.Uid);
КонецПроцедуры
